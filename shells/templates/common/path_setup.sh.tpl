SKILLS_ROOT_DIR="${AGENT_WORKSPACE}/skills"
SKILL_DIR="${SKILLS_ROOT_DIR}/${SKILL_DIR_NAME}"
AGENTS_FILE="${AGENT_WORKSPACE}/AGENTS.md"
SOUL_FILE="${AGENT_WORKSPACE}/SOUL.md"
IDENTITY_FILE="${AGENT_WORKSPACE}/IDENTITY.md"
TASK_CLI_PATH="${SKILL_DIR}/scripts/task-cli.py"
SKILL_MD_PATH="${SKILL_DIR}/SKILL.md"
SKILL_BUNDLE_ARCHIVE="${AGENT_WORKSPACE}/${SKILL_DIR_NAME}.zip"

mkdir -p "${AGENT_WORKSPACE}"
mkdir -p "${SKILLS_ROOT_DIR}"
